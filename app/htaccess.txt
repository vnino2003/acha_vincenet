<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /

    # Kung file o folder talaga, wag i-rewrite
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d

    # Lahat ng iba, ipasa kay index.php
    RewriteRule ^(.*)$ index.php?/$1 [L]
</IfModule>
